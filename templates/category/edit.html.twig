{% extends 'base.html.twig' %}


{% block title %}
	Edition de
	{{category.name}}
{% endblock %}

{% block body %}
	<h1>Editer le produit
		{{category.name}}</h1>
	<h1>Modification de la catégrie</h1>


	{{ form_start(formView) }}

	{{ form_errors(formView) }}


	<div class="row">
		<div class="col">
			{{ form_row(formView.name) }}

		</div>
		<button type="submit" class="btn btn-primary">
			<i class="fas fa-save"></i>
			Enregistrer
		</button>

		{{form_end(formView)}}

	{% endblock %}
