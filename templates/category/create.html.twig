{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
	<h1>Créer un catégory</h1>

	{{form_start(formView)}}

	{{form_widget(formView)}}

	<button type="submit" class="btn btn-primary">
		<i class="fas fa-save"></i>
		Créer une categorie
	</button>
	{{form_end(formView)}}


{% endblock %}
